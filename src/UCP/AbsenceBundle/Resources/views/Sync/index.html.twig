{% extends 'UCPAbsenceBundle::layout.html.twig' %}

{% block body %}
<h1>Google calendar sync</h1>

{% if account_email is not null %}
Synchronisation avec Google calendar activée sur {{ account_email }} avec le calendrier "{{ calendar_name }}".<br>
<a href="{{ path('sync_unlink') }}">Dissocier le compte</a> ou <a href="{{ path('sync_calendar_choice') }}">changer de calendrier</a>.
{% else %}
Aucun compte Google lié, cliquez <a href="{{ path('sync_link') }}">ici</a> pour lier unn compte.
{% endif %}
<br>
<i>Dernière synchronisation {{ last_sync|date() }}.

{% endblock %}